'use strict';

/*
 * Application settings
 */

export const port = process.env.PORT || 3000;
export const host = process.env.HOST_IP || 'localhost';

export const ipInfoKey = process.env.IP_INFO_KEY;
